import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="relative mx-auto w-full max-w-screen-lg overflow-auto">
      <div class="relative min-h-screen px-5 pb-16 pt-3">
        <div>
          <p>Hi</p>
        </div>
      </div>
    </div>
  );
});
